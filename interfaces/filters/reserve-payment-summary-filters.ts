/**
 * Espejo de `ReservePaymentSummaryFilterRequestDto` del backend.
 * El wrapper no expone campos adicionales — el filtrado es por `reserveId` en la ruta.
 */
export interface ReservePaymentSummaryFilters {}

export const emptyReservePaymentSummaryFilters: ReservePaymentSummaryFilters = {};
